# Sprint — 2026-02-25

## Objective
Ship the interactive puzzle Face 4: fixed 280×280 board, translucent header overlay, gold interaction colors, spec-compliant result cards, promotion picker column, and CTA hover animation.

## Task → Agent Assignment

| Task ID | Agent | Files Owned | Status | Commit |
|---------|-------|-------------|--------|--------|
| S4-1    | A     | DesignTokens.swift | ✓ done | ef0d488 |
| S4-11   | B     | InfoPanelView.swift | ✓ done | 61f5d72 |
| S4-3    | C     | GoldRingLayerView.swift | ✓ done | 5f59127 |
| S4-2    | D     | InteractiveBoardView.swift | ✓ done | 531b577 |
| S4-9    | D     | PromotionPickerView.swift, InteractiveBoardView.swift | ✓ done | 93c6e40 |
| S4-4    | E     | GuessMoveView.swift | ✓ done | 23cf909 |
| S4-5    | E     | GuessMoveView.swift | ✓ done | e164378 |
| S4-6    | E     | GuessMoveView.swift | ✓ done | 8bbb1a0 |
| S4-7    | E     | GuessMoveView.swift, InteractiveBoardView.swift | ✓ done | ff240aa |
| S4-8    | E     | GuessMoveView.swift | ✓ done | 2e35484 |
| S4-10   | F     | GuessMoveView.swift, BoardView.swift, InteractiveBoardView.swift | ✓ done | 1a29734 |

## Dependency Graph
```
S4-1 (tokens) ──→ S4-2 (InteractiveBoardView colors)
              └──→ S4-3 (tick extension)
              └──→ S4-4 (GuessMoveView layout) ──→ S4-5 (header overlay)
                                               └──→ S4-6 (remove text) ──→ S4-10 (opponent anim)
                                               └──→ S4-7 (wrong move feedback)
                                               └──→ S4-8 (result cards)
              └──→ S4-9 (promotion picker)
S4-11 (CTA hover) — INDEPENDENT

Wave 1 (no deps, run in parallel): S4-1, S4-11
Wave 2 (after S4-1 completes):    S4-2+S4-9 (Agent D, sequential), S4-3 (Agent C, parallel)
Wave 3 (after Wave 2):            S4-4 → S4-5 → S4-6 → S4-7 → S4-8 (Agent E, sequential)
Wave 4 (after S4-6 in Wave 3):    S4-10 (Agent F, after Agent E finishes)
```

## Interface Contracts

### DesignTokens (produced by S4-1, consumed by all dependent tasks):
```swift
enum ChessClockRadius {
    // NEW:
    static let puzzleBoard: CGFloat = 4
    // unchanged: outer(18), ring(12), card(12), pill(8), board(8), badge(4)
}
enum ChessClockSize {
    // CHANGED:
    static let tickLength: CGFloat = 12  // was 8
    // unchanged: all other constants
}
```

### PromotionPickerView (changed by S4-9, consumed by InteractiveBoardView):
```swift
struct PromotionPickerView: View {
    let color: PieceColor
    let promotionFile: Int       // NEW — 0-7 file index
    let isFlipped: Bool          // NEW
    let onPick: (PieceType) -> Void
}
```
Column x = `CGFloat(promotionFile) * ChessClockSize.square`. No title text. VStack(spacing:1), ultraThinMaterial cells.

### BoardView (extended by S4-10):
```swift
struct BoardView: View, Equatable {
    let fen: String
    var isFlipped: Bool = false
    var highlightedSquares: (from: ChessSquare, to: ChessSquare)? = nil  // NEW optional
}
```

### InteractiveBoardView (extended by S4-10):
```swift
struct InteractiveBoardView: View {
    let fen: String
    let isFlipped: Bool
    var highlightedSquares: (from: ChessSquare, to: ChessSquare)? = nil  // NEW optional
    let onMove: (ChessMove) -> Void
}
```

### GuessMoveView after S4-4 (ZStack root, 280×280 board):
- Root body is `ZStack` (not `VStack`)
- `boardSection` uses `InteractiveBoardView`/`BoardView` at `.frame(width: 280, height: 280)`, clipped with `ChessClockRadius.puzzleBoard` (4pt)
- Header overlay is a `ZStack(alignment: .top)` layer on top of the board

## Agent Log
- Wave 1: Agent A launched (background) — S4-1
- Wave 1: Agent B launched (background) — S4-11

## Issues & Adaptations
(none yet)

## Integration Checklist
- [x] All agents committed their work (git log shows all 11 task commits)
- [x] Full build succeeded after merging all files (BUILD SUCCEEDED)
- [x] Senior integration tasks done (S4-10 done as senior foreground)
- [x] All Verify: commands from TODO.md pass (xcodebuild: BUILD SUCCEEDED for each task)
- [ ] Sprint file archived
