# Sprint 5 — 2026-02-25 — Puzzle Visual Overhaul & Polish

## Objective
Fix InfoPanel centering, overhaul puzzle header pills (flash bug, styling, logic), add decorative marble noise ring to puzzle mode with color-transition feedback, and redesign result overlays as compact frosty cards.

## Task → Agent Assignment

| Task ID | Agent | Files Owned | Status | Commit |
|---------|-------|-------------|--------|--------|
| S5-1    | Senior (inline) | InfoPanelView.swift | complete | c679bcb |
| S5-2    | A (Wave 1) | GoldNoiseShader.metal, GoldNoiseRenderer.swift | complete | 541ea1d |
| S5-3    | B (Wave 1) | DesignTokens.swift | complete | 541ea1d |
| S5-4    | C (Wave 2) | PuzzleRingView.swift (new) | complete | 34d1183 |
| S5-5    | D (Wave 2) | GuessMoveView.swift | complete | b54c71f |
| S5-6    | Senior | ClockView.swift, GuessMoveView.swift | complete | c34dfd5 |
| S5-7    | Senior | GuessMoveView.swift | complete | 04764c0 |

## Dependency Graph
```
S5-1 (InfoPanel centering)     ─── INDEPENDENT
S5-2 (Shader color scheme)     ──► S5-4 (PuzzleRingView)
S5-3 (DesignTokens)            ──► S5-4 (PuzzleRingView), S5-5 (Header pills)
S5-4 + S5-5 are INDEPENDENT of each other after their deps resolve
S5-6 (Ring integration)        DEPENDS ON S5-4, S5-5
S5-7 (Result overlay)          DEPENDS ON S5-6

Wave 1: S5-1 (inline), S5-2, S5-3 — senior + 2 parallel agents
Wave 2: S5-4, S5-5 — 2 parallel agents (after Wave 1)
Wave 3: S5-6 — senior (after Wave 2)
Wave 4: S5-7 — senior (after Wave 3)
```

## Interface Contracts

### GoldNoiseRenderer new properties (S5-2)
```swift
// Added to GoldNoiseRenderer class
var colorScheme: Float = 0    // 0=gold, 1=marble
var tintR: Float = 0
var tintG: Float = 0
var tintB: Float = 0
var tintStrength: Float = 0
```

### GoldNoiseShader.metal new buffers (S5-2)
```metal
// Added buffer params after speed:
constant float &colorScheme [[buffer(3)]]
constant float &tintR [[buffer(4)]]
constant float &tintG [[buffer(5)]]
constant float &tintB [[buffer(6)]]
constant float &tintStrength [[buffer(7)]]
```

### DesignTokens additions (S5-3)
```swift
// ChessClockColor
static let pillBackground = Color(white: 0.08).opacity(0.70)
static let pillBorder = Color.white.opacity(0.15)
static let ringTintWrong = (r: CGFloat(0.85), g: CGFloat(0.2), b: CGFloat(0.15))
static let ringTintCorrect = (r: CGFloat(0.2), g: CGFloat(0.75), b: CGFloat(0.3))

// New enum
enum ChessClockTiming {
    static let headerAutoHide: Double = 2.5
    static let wrongTriesDisplay: Double = 3.5
    static let feedbackRampUp: Double = 0.15
    static let feedbackHold: Double = 1.0
    static let feedbackRampDown: Double = 0.20
    static let feedbackTintPeak: Float = 0.7
    static let pulseDipDuration: Double = 0.12
    static let pulseDipFloor: Float = 0.20
}
```

### TintTarget enum (S5-4, file scope in PuzzleRingView.swift)
```swift
enum TintTarget: Equatable { case none, wrong, correct }
```

### PuzzleRingView signature (S5-4)
```swift
struct PuzzleRingView: NSViewRepresentable {
    let isActive: Bool
    let tintTarget: TintTarget
    let tintSeq: Int
}
```

### GuessMoveView.onFeedback callback (S5-6)
```swift
// Added to GuessMoveView
let onFeedback: ((Bool) -> Void)?  // true=correct, false=wrong
```

## Agent Log
- Wave 1: S5-1 done inline (senior), S5-2+S5-3 agents launched in parallel
- Wave 1 complete: S5-2 and S5-3 both landed in 541ea1d (agent B committed both)
- S5-1 committed as c679bcb
- Wave 2: S5-4 and S5-5 agents launched in parallel
- Wave 2 complete: S5-4 = 34d1183, S5-5 = b54c71f
- Wave 3: S5-6 done by senior (integration) — c34dfd5
- Wave 4: S5-7 done by senior (overlay redesign) — 04764c0
- Final build: BUILD SUCCEEDED

## Issues & Adaptations
- GoldNoiseRenderer.swift has pre-existing unstaged change: render size 150→200 (from previous session). Agents will work on top of this.
- DESIGN.md has pre-existing unstaged change: sprint renumbering (5→6, 6→7).

## Integration Checklist
[x] All agents committed their work
[x] Full build succeeded after merging all files
[x] Senior integration tasks done (S5-6, S5-7)
[x] All Verify: commands pass (BUILD SUCCEEDED on all)
[x] Sprint file archived
