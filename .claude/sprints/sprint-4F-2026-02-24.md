# Sprint 4F — 2026-02-24

## Objective
Fix the broken ring rendering from Sprint 4R — strip back to a working 8pt gold ring with noise-like color drift, no rotation/blur/glow.

## Task → Agent Assignment

| Task ID | Agent  | Files Owned                                        | Status  | Commit |
|---------|--------|----------------------------------------------------|---------|--------|
| S4F-1   | A      | ChessClock/ChessClock/Views/GoldRingLayerView.swift | pending | —      |
| S4F-2   | A      | ChessClock/ChessClock/Views/GoldRingLayerView.swift | pending | —      |
| S4F-3   | Senior | ChessClock/ChessClock/Views/ClockView.swift         | pending | —      |
| S4F-4   | Senior | ChessClock/ChessClock/Views/CLAUDE.md               | pending | —      |

## Dependency Graph
```
S4F-1 (foundation rewrite) ──► S4F-2 (color drift), S4F-3 (ClockView fix), S4F-4 (docs)
S4F-2 DEPENDS ON S4F-1 (same file, sequential)
S4F-3 DEPENDS ON S4F-1 (needs working GoldRingLayerView)
S4F-4 DEPENDS ON S4F-1 (describes final architecture)
S4F-3 and S4F-4 are INDEPENDENT of each other after S4F-1
```

## Interface Contracts

### GoldRingLayerView (public API — unchanged)
```swift
struct GoldRingLayerView: NSViewRepresentable {
    let minute: Int
    let second: Int
}
```

### ChessClockRadius (from DesignTokens.swift — read only)
```swift
enum ChessClockRadius {
    static let outer: CGFloat = 18  // ring path: outerRadius = 18-2=16, innerRadius = 18-10=8
}
```

### Target Layer Hierarchy (Sprint 4F spec)
```
FlippedLayerView (isFlipped = true)
  ├─ trackLayer: CAShapeLayer              — gray 15% ring (even-odd, static)
  ├─ goldContainer: CALayer                — masked by progressMask (pie wedge)
  │   ├─ gradientLayer: CAGradientLayer    — .conic, 17 gold stops, ring-masked, locations drift
  │   ├─ specularStrip: CAShapeLayer       — white 20% inner highlight (static)
  │   └─ shadowStrip: CAShapeLayer         — black 8% outer shadow (static)
  └─ ticksLayer: CALayer                   — 4 cardinal ticks (static, always on top)
```

**Key change from 4R:** gradientLayer gets a direct ring-shaped CAShapeLayer mask (no gradientClipContainer). Progress mask is on goldContainer. No glow system. No rotation. Only locations drift.

## Agent Log

## Issues & Adaptations

## Integration Checklist
- [ ] All agents committed their work
- [ ] Full build succeeded after merging all files
- [ ] Senior integration tasks done (S4F-3, S4F-4)
- [ ] All Verify: commands from TODO.md pass
- [ ] Sprint file archived
